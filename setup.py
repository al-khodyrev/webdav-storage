from setuptools import setup, find_packages
setup(
    name = "webdav-storage",
    version = "0.0.3",
    packages = find_packages(),
    author = "StimuL",
    author_email = "neostimu@gmail.com",
    description = "This module implements WebDAV storage for django 2.0 framework and with python 3 support"
)
